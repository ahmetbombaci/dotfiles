- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.aliases:
    ~/.bash_aliases:
    ~/.bash_functions:
    ~/.bashrc:
    ~/bin:
    /etc/conky/conky.conf:
      path: conky.conf
    ~/.config/alacritty/alacritty.yml:
    ~/.cvsignore:
    ~/.dotfiles: ''
    ~/.exports:
    ~/.functions:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.profile:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.zshrc:
