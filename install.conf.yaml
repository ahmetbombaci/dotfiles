- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.aliases:
    ~/.bash_aliases:
    ~/.bash_functions:
    ~/.bashrc:
    ~/bin:
    /etc/conky/conky.conf:
      path: conky.conf
    ~/.cvsignore:
    ~/.dotfiles: ''
    ~/.exports:
    ~/.functions:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.profile:
    ~/.vimrc:
    ~/.zshrc:
